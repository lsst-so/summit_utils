# Installed by the "Run mypy" workflow only; runtime deps live in
# requirements.txt. Without the requests stubs, mypy.ini's per-module
# ignore_missing_imports silently degrades every requests object to Any.
pytest
responses
types-requests
# Without these, @deprecated from deprecated.sphinx is untyped, which
# erases the signature of every function it decorates - including
# makeDefaultLatissButler, leaving the whole butler test case unchecked.
types-Deprecated
